# 📚 My Keeps - সম্পূর্ণ ব্যাকএন্ড আর্কিটেকচার ও টেকনিক্যাল ডকুমেন্টেশন

> **অ্যাপ্লিকেশনের ধরন:** RESTful API Server  
> **টেকনোলজি স্ট্যাক:** Node.js, Express.js, TypeScript, MongoDB Atlas, Mongoose, JWT, Bcrypt  
> **লাইভ সার্ভার ইউআরএল:** `https://my-keeps-server.onrender.com`  
> **গিটহাব রিপোজিটরি:** `https://github.com/Asmual/My-Keeps--Server.git`

---

## ১. ব্যাকএন্ড পরিচিতি ও আর্কিটেকচার (Architecture Overview)

My Keeps ব্যাকএন্ডটি একটি আধুনিক, স্কেলেবল এবং সিকিউর **Modular Layered Architecture (MVC প্যাটার্ন)** অনুসরণ করে তৈরি করা হয়েছে:

```
                Client (Frontend / Vercel / Postman)
                               │
                               ▼
                 [app.ts] (Middleware, CORS, JSON)
                               │
                ┌──────────────┴──────────────┐
                ▼                             ▼
        Routes (routes/)             Public Routes (/health)
                │
                ▼
        Middlewares (middlewares/)   (যেমন: JWT Authentication)
                │
                ▼
        Controllers (controllers/)   (রিকোয়েস্ট প্রসেসিং ও রেসপন্স)
                │
                ▼
        Models (models/)             (Mongoose Data Schemas)
                │
                ▼
        MongoDB Atlas (Database)
```

- **Separation of Concerns:** প্রতিটি কাজের জন্য আলাদা লেয়ার রয়েছে (রাউটিং, কন্ট্রোলিং, ডেটাবেস স্কিমা, সিকিউরিটি মিডলওয়্যার)।
- **Type Safety:** সম্পূর্ণ প্রজেক্টটি TypeScript দিয়ে টাইপ-সেফ করা হয়েছে, ফলে রানটাইম এররের ঝুঁকি নেই।
- **Decoupled:** ফ্রন্টএন্ড থেকে স্বাধীনভাবে চলতে সক্ষম এবং যেকোনো ক্লায়েন্ট অ্যাপের সাথে সহজেই কানেক্ট করা যায়।

---

## ২. ফোল্ডার স্ট্রাকচার ও কোন ফোল্ডার কেন করা হয়েছে?

```
my-keeps-backend/
├── dist/                     # কম্পাইল করা প্রোডাকশন জাভাস্ক্রিপ্ট ফাইল
├── src/                      # মূল TypeScript সোর্স কোড
│   ├── config/               # ডেটাবেস ও কনফিগারেশন লজিক
│   ├── controllers/          # এপিআই-এর বিজনেস লজিক ও রিকোয়েস্ট হ্যান্ডলার
│   ├── middlewares/          # অথেন্টিকেশন ও প্রটেকশন মিডলওয়্যার
│   ├── models/               # MongoDB Mongoose ডেটাবেস স্কিমা ও টাইপস
│   ├── routes/               # এন্ডপয়েন্ট ইউআরএল ডিক্লেয়ারেশন
│   ├── app.ts                # এক্সপ্রেস অ্যাপ্লিকেশন কনফিগারেশন
│   └── server.ts             # সার্ভার বুটস্ট্র্যাপিং ও এন্ট্রি পয়েন্ট
├── .env                      # সিক্রেট এনভায়রনমেন্ট ভেরিয়েবল (লোকাল)
├── .env.example              # ভেরিয়েবলের ডেমো টেমপ্লেট
├── .gitignore                # গিটের জন্য এক্সক্লুশন রুলস
├── package.json              # ডিপেন্ডেন্সি ও স্ক্রিপ্টস কনফিগারেশন
├── render.yaml               # Render ক্লাউড ডেপ্লয়মেন্ট ব্লুপ্রিন্ট
├── tsconfig.json             # TypeScript কম্পাইলার কনফিগারেশন
└── BACKEND_DOCUMENTATION.md  # সম্পূর্ণ সিস্টেম ডকুমেন্টেশন
```

### ফোল্ডারগুলোর উদ্দেশ্য:
1. **`src/`:** সমস্ত ডেভেলপমেন্ট TypeScript কোড এই ফোল্ডারে থাকে।
2. **`src/config/`:** ডেটাবেস কানেকশন (MongoDB Atlas) বা থার্ড পার্টি সার্ভিসের প্রাথমিক কনফিগারেশন আলাদা রাখতে এটি তৈরি করা হয়েছে।
3. **`src/models/`:** MongoDB-তে কী কী ডেটা জমা হবে, তাদের টাইপ, ভ্যালিডেশন এবং ডিফল্ট মান কেমন হবে তা ডিক্লেয়ার করার জন্য।
4. **`src/controllers/`:** আসল বিজনেস লজিক (ডেটা সেভ, আপডেট, ডিলিট, ক্যালকুলেশন, পাসওয়ার্ড ম্যাচিং ইত্যাদি) এই ফোল্ডারের ফাইলগুলোতে থাকে।
5. **`src/middlewares/`:** কোনো রিকোয়েস্ট কন্ট্রোলারে যাওয়ার আগে সেটির সিকিউরিটি যাচাই করার জন্য (যেমন: JWT টোকেন ভ্যালিড কি না)।
6. **`src/routes/`:** কোন ইউআরএল পাথে কোন কন্ট্রোলার ফাংশনটি কল হবে তা সুনির্দিষ্টভাবে ম্যাপ করার জন্য।
7. **`dist/`:** `tsc` বিল্ড করার পর তৈরি হওয়া পিওর JavaScript ফাইলসমূহ, যা ক্লাউড সার্ভারে (Render) সরাসরি রান করে।

---

## ৩. প্রতিটি ফাইল পরিচিতি ও কেন বানানো হয়েছে?

### রুট ডিরেক্টরির ফাইলসমূহ:

| ফাইল | কেন তৈরি করা হয়েছে? (Purpose) |
| :--- | :--- |
| **`server.ts`** | **এন্ট্রি পয়েন্ট:** সার্ভারের প্রধান স্টার্টার। এটি ডেটাবেসে কানেক্ট করে, এক্সপ্রেস অ্যাপ চালু করে নির্দিষ্ট পোর্টে লিসেন করে এবং graceful shutdown হ্যান্ডেল করে। |
| **`app.ts`** | **এক্সপ্রেস কারখানা:** এক্সপ্রেস অ্যাপ তৈরি করে। এখানে CORS, JSON বডি পার্সার, হেলথ চেক রুট, এরর হ্যান্ডলার এবং মূল API রুটগুলো মাউন্ট করা হয়েছে। |
| **`package.json`** | প্রজেক্টের নাম, স্ক্রিপ্ট (`build`, `start`, `postinstall`), এবং ইনস্টল করা সমস্ত প্যাকেজের তালিকা সংরক্ষণ করে। |
| **`tsconfig.json`** | TypeScript কোড কীভাবে জাভাস্ক্রিপ্টে কম্পাইল হবে (যেমন: `ES2022`, `commonjs`, `outDir: ./dist`) তা নিয়ন্ত্রণ করে। |
| **`render.yaml`** | Render ক্লাউডে যাতে কোনো ম্যানুয়াল ঝামেলা ছাড়া স্বয়ংক্রিয়ভাবে সঠিক বিল্ড ও রান কমান্ড দিয়ে সার্ভার ডেপ্লয় হতে পারে। |
| **`.env.example`** | অন্য ডেভেলপাররা যাতে বুঝতে পারে কোন কোন এনভায়রনমেন্ট ভেরিয়েবল সেট করতে হবে। |
| **`.gitignore`** | অপ্রয়োজনীয় বা সিক্রেট ফাইল (`node_modules`, `.env`) গিটে পুশ হওয়া রোধ করার জন্য। |

---

### `src/config/` ফাইল:
- **`db.ts`:**
  - **কাজ:** Mongoose ব্যবহার করে MongoDB Atlas ক্লাউডের সাথে সুরক্ষিত কানেকশন তৈরি করে।
  - **কেন দরকার:** ডেটাবেস ইউআরআই না থাকলে প্রসেস এক্সিট করা এবং কানেকশন ডিসকানেক্ট হলে লগ প্রিন্ট করার দায়িত্ব পালন করে।

---

### `src/models/` ফাইলসমূহ:
1. **`Note.ts`:**
   - **কাজ:** নোট সম্পর্কিত স্কিমা।
   - **ফিল্ডসমূহ:**
     - `title`: নোটের শিরোনাম।
     - `content`: নোটের বডি টেক্সট।
     - `color`: নোটের ব্যাকগ্রাউন্ড কালার (`coral`, `mint`, `fog`, ইত্যাদি)।
     - `isPinned`: নোট পিন করা আছে কিনা।
     - `isImportant`: গুরুত্বপূর্ণ ফ্ল্যাগ।
     - `isArchived`: আর্কাইভ ফ্ল্যাগ।
     - `isTrashed`: ট্র্যাশ ফ্ল্যাগ (সফট ডিলিট)।
     - `labels`: ট্যাগের অ্যারে।
     - `checklist`: চেকলিস্ট আইটেমের সাব-ডকুমেন্ট অ্যারে (`[{ id, text, completed }]`)।
     - `noteType`: নোটের ধরন (`'text' | 'checklist' | 'image' | 'voice'`)।
     - `images`: ক্লাউডিনারি ইমেজের ইউআরএল অ্যারে।
     - `audioUrl`: অডিও/ভয়েস নোটের ইউআরএল।
     - `reminder`: রিমাইন্ডারের তারিখ ও সময়।
     - `userId`: কোন ইউজারের নোট তা ট্র্যাক করার আইডি।
     - `timestamps`: `createdAt` ও `updatedAt` অটোমেটিক যোগ হয়।
   - **স্পেশাল ফিচার:** `toJSON` ট্রান্সফর্মেশনের মাধ্যমে MongoDB-এর `_id`-কে ফ্রন্টএন্ড ফ্রেন্ডলি `id`-তে কনভার্ট করে দেওয়া হয়।

2. **`User.ts`:**
   - **কাজ:** ইউজারের প্রোফাইল ও ক্রেডেনশিয়াল সংরক্ষণের স্কিমা।
   - **ফিল্ডসমূহ:** `name`, `email` (unique, lowercase), `password` (bcrypt hashed), `image`, `gender`, `phoneNumber`, `timestamps`।

---

### `src/middlewares/` ফাইল:
- **`authMiddleware.ts`:**
  - **ফাংশন:** `authenticateJWT`
  - **কাজ:** রিকোয়েস্টের হেডার থেকে `Authorization: Bearer <token>` বের করে।
  - `jwt.verify` দিয়ে টোকেন ডিকোড করে ইউজারের সত্যতা যাচাই করে এবং `req.user`-এ ইউজার ডেটা যুক্ত করে দেয়। টোকেন ভুল বা মেয়াদোত্তীর্ণ হলে `401 Unauthorized` রিটার্ন করে।

---

### `src/controllers/` ফাইলসমূহ:
1. **`authController.ts`:**
   - **`register`:** নাম, ইমেইল ও পাসওয়ার্ড যাচাই করে, ইমেইল ডুপ্লিকেট কিনা চেক করে, `bcrypt` দিয়ে পাসওয়ার্ড হ্যাশ করে নতুন ইউজার তৈরি করে এবং JWT টোকেন প্রদান করে।
   - **`login`:** ইমেইল দিয়ে ইউজার খুঁজে বের করে, `bcrypt.compare` দিয়ে পাসওয়ার্ড মেলায় এবং ভ্যালিড হলে JWT টোকেন রিটার্ন করে।
   - **`getMe`:** লগইন করা ইউজারের নিজস্ব প্রোফাইল ডেটা রিটার্ন করে।

2. **`noteController.ts`:**
   - **`getNotes`:** কুয়েরি প্যারামিটার অনুযায়ী নোট খোঁজে (যেমন: `archive`, `trash`, `checklist`, `important`, `image`, `voice`, `search`, `label`, `userId`) এবং পিন ও আপডেটের সময় অনুযায়ী সাজিয়ে রিটার্ন করে।
   - **`getNoteById`:** নির্দিষ্ট একটি নোটের বিস্তারিত তথ্য নিয়ে আসে।
   - **`createNote`:** নতুন নোট তৈরি করে ডেটাবেসে সেভ করে।
   - **`updateNote`:** নোটের যেকোনো তথ্য আংশিক বা পূর্ণ আপডেট করে।
   - **`deleteNote`:** নির্দিষ্ট আইডি ধরে ডেটাবেস থেকে পার্মানেন্টলি ডিলিট করে।
   - **`emptyTrash`:** ট্র্যাশে থাকা সমস্ত নোট একবারে ক্লিন করে।
   - **`deleteNotes`:** ফ্রন্টএন্ড থেকে পাঠানো ব্যাচ আইডি বা `action=empty-trash` হ্যান্ডেল করে একবারে একাধিক নোট ডিলিট করে।

3. **`userController.ts`:**
   - **`getUserProfile`:** `userId` বা `email` দিয়ে ইউজারের প্রোফাইল ফেচ করে।
   - **`updateUserProfile`:** ইউজারের নাম, ছবি, জেন্ডার এবং ফোন নম্বর আপডেট করে।

---

### `src/routes/` ফাইলসমূহ:
1. **`authRoutes.ts`:** `/api/auth` প্রিফিক্সে রেজিস্ট্রেশন, লগইন এবং প্রোফাইল ভেরিফিকেশন রাউট পরিচালনা করে।
2. **`noteRoutes.ts`:** `/api/notes` প্রিফিক্সে নোটের সমস্ত CRUD অপারেশন পরিচালনা করে।
3. **`userRoutes.ts`:** `/api/user/profile` প্রিফিক্সে প্রোফাইল ফেচ ও আপডেট পরিচালনা করে।

---

## ৪. রাউটস এবং এপিআই এন্ডপয়েন্ট রেফারেন্স (API Endpoints Table)

### ক. হেলথ ও সিস্টেম রাউট
| মেথড | ইউআরএল পাথ | অথেন্টিকেশন | বিবরণ |
| :--- | :--- | :--- | :--- |
| `GET` | `/` | পাবলিক | সার্ভারের স্টেটাস ও ভার্সন রেসপন্স দেয় |
| `GET` | `/health` | পাবলিক | রেন্ডার বা মনিটরিং সার্ভিসের জন্য হেলথ চেক |

---

### খ. অথেন্টিকেশন রাউটস (`/api/auth`)
| মেথড | ইউআরএল পাথ | হেডার / বডি প্যারামিটার | বিবরণ |
| :--- | :--- | :--- | :--- |
| `POST` | `/api/auth/register` | Body: `{ name, email, password }` | নতুন ইউজার তৈরি ও JWT টোকেন রিটার্ন |
| `POST` | `/api/auth/login` | Body: `{ email, password }` | লগইন যাচাই ও JWT টোকেন রিটার্ন |
| `GET` | `/api/auth/me` | Header: `Authorization: Bearer <token>` | লগইন থাকা ইউজারের তথ্য রিড করা |

---

### গ. নোটস ম্যানেজমেন্ট রাউটস (`/api/notes`)
| মেথড | ইউআরএল পাথ | কুয়েরি / বডি প্যারামিটার | বিবরণ |
| :--- | :--- | :--- | :--- |
| `GET` | `/api/notes` | Query: `filter`, `userId`, `search`, `label` | ফিল্টারকৃত সকল নোটের তালিকা নিয়ে আসা |
| `POST` | `/api/notes` | Body: `{ title, content, color, noteType, images, ... }` | নতুন নোট তৈরি করা |
| `GET` | `/api/notes/:id` | Param: `id` | নির্দিষ্ট একটি নোট দেখা |
| `PATCH` | `/api/notes/:id` | Param: `id`, Body: `{ ...updates }` | নোটের তথ্য আপডেট করা |
| `DELETE` | `/api/notes/:id` | Param: `id` | একটি নির্দিষ্ট নোট চিরতরে মুছে ফেলা |
| `DELETE` | `/api/notes` | Query: `?action=empty-trash` অথবা Body: `{ ids: [] }` | ট্র্যাশ খালি করা বা একসাথে একাধিক নোট ডিলিট করা |
| `DELETE` | `/api/notes/trash/empty` | Query: `?userId=...` | ট্র্যাশ খালি করার অল্টারনেটিভ রুট |

---

### ঘ. ইউজার প্রোফাইল রাউটস (`/api/user`)
| মেথড | ইউআরএল পাথ | কুয়েরি / বডি প্যারামিটার | বিবরণ |
| :--- | :--- | :--- | :--- |
| `GET` | `/api/user/profile` | Query: `?userId=...` বা `?email=...` | ইউজারের প্রোফাইল তথ্য ফেচ করা |
| `PATCH` | `/api/user/profile` | Body: `{ userId, name, image, gender, phoneNumber }` | প্রোফাইল আপডেট করা |

---

## ৫. গুরুত্বপূর্ণ ফাংশনসমূহ এবং তাদের কাজের বিবরণ

| ফাংশনের নাম | ফাইলের অবস্থান | কাজের বিস্তারিত বিবরণ |
| :--- | :--- | :--- |
| `connectDB()` | `src/config/db.ts` | `mongoose.connect()` দিয়ে MongoDB Atlas-এর সাথে কানেকশন খোলে এবং কানেকশন ফেইলিওর হলে সেফলি হ্যান্ডেল করে। |
| `createApp()` | `src/app.ts` | এক্সপ্রেস অ্যাপ ইনিশিয়ালাইজ করে, CORS রুল সেট করে এবং সব রাউট ও গ্লোবাল এরর হ্যান্ডলার মাউন্ট করে। |
| `startServer()` | `src/server.ts` | `connectDB()` সম্পন্ন হওয়ার পর নির্দিষ্ট পোর্টে সার্ভার লিসেন করায় এবং `SIGINT`/`SIGTERM` সিগন্যালে গ্রেসফুল শাটডাউন করে। |
| `generateToken()` | `src/controllers/authController.ts` | ইউজার আইডি ও ইমেইলকে ৭ দিনের জন্য সিক্রেট কি দিয়ে এনক্রিপ্ট করে JWT স্ট্রিং তৈরি করে। |
| `register()` | `src/controllers/authController.ts` | পাসওয়ার্ডকে ১০ রাউন্ড সল্ট দিয়ে হ্যাশ করে নতুন ইউজার তৈরি করে। |
| `login()` | `src/controllers/authController.ts` | ডেটাবেসের হ্যাশড পাসওয়ার্ডের সাথে ইউজারের পাসওয়ার্ড `bcrypt.compare()` দিয়ে চেক করে। |
| `authenticateJWT()` | `src/middlewares/authMiddleware.ts` | বিয়ারার টোকেন ডিকোড করে ইনভ্যালিড টোকেন হলে সাথে সাথে এরর রেসপন্স পাঠিয়ে কন্ট্রোলার প্রটেক্ট করে। |
| `getNotes()` | `src/controllers/noteController.ts` | রিকোয়েস্টের প্যারামিটার অনুযায়ী ডায়নামিক কুয়েরি অবজেক্ট বানিয়ে মঙ্গোডিবি থেকে নোট ফেচ করে। |
| `deleteNotes()` | `src/controllers/noteController.ts` | ব্যাচ ডিলিট বা ট্র্যাশ এম্পটি অ্যাকশন হ্যান্ডেল করে। |
| `updateUserProfile()` | `src/controllers/userController.ts` | ইউজারের নাম, ছবি, জেন্ডার ও ফোন নম্বর `findOneAndUpdate()` মেথডে সেভ করে। |

---

## ৬. ইনস্টল করা এনপিএম প্যাকেজসমূহ (NPM Packages & Dependencies)

### প্রোডাকশন ডিপেন্ডেন্সিস (Production Dependencies):

1. **`express` (`^5.2.1`):**
   - **কেন ব্যবহার করা হয়েছে:** Node.js-এর সবচেয়ে জনপ্রিয় ওয়েব ফ্রেমওয়ার্ক। এটি সহজে রাউটিং, মিডলওয়্যার এবং এইচটিটিপি রিকোয়েস্ট/রেসপন্স হ্যান্ডেল করতে সাহায্য করে।
2. **`mongoose` (`^9.9.5`):**
   - **কেন ব্যবহার করা হয়েছে:** MongoDB-এর জন্য Object Data Modeling (ODM) লাইব্রেরি। এটি ডেটাবেস স্কিমা তৈরি, ডেটা ভ্যালিডেশন এবং কুয়েরি রান করতে ব্যবহৃত হয়।
3. **`dotenv` (`^17.4.2`):**
   - **কেন ব্যবহার করা হয়েছে:** প্রজেক্টের রুট থেকে `.env` ফাইলের এনভায়রনমেন্ট ভেরিয়েবলগুলো পড়ে `process.env`-তে লোড করে।
4. **`cors` (`^2.8.6`):**
   - **কেন ব্যবহার করা হয়েছে:** Cross-Origin Resource Sharing নিশ্চিত করে। এর মাধ্যমে Vercel ফ্রন্টএন্ড বা লোকালহোস্ট থেকে পাঠানো এপিআই কল ব্যাকএন্ড ব্লক না করে অ্যাকসেপ্ট করে।
5. **`bcrypt` (`^6.0.0`):**
   - **কেন ব্যবহার করা হয়েছে:** ইউজারের পাসওয়ার্ড প্লেইন টেক্সট হিসেবে সেভ না করে ক্রিপ্টোগ্রাফিক সল্ট সহ ওয়ান-ওয়ে হ্যাশিং করার জন্য।
6. **`jsonwebtoken` (`^9.0.3`):**
   - **কেন ব্যবহার করা হয়েছে:** স্টেটলেস এবং সুরক্ষিত প্রমাণীকরণের (Authentication) জন্য JSON Web Tokens (JWT) তৈরি এবং যাচাই করতে ব্যবহৃত হয়।
7. **`typescript` (`^7.0.2`):**
   - **কেন `dependencies`-এ রাখা হয়েছে:** Render ক্লাউডে প্রোডাকশন মোডে যাতে `npm install` করার সময় টাইপস্ক্রিপ্ট মিসিং না হয় এবং `postinstall`-এ `tsc` কমান্ড সরাসরি রান করতে পারে।
8. **`@types/express`, `@types/cors`, `@types/node`, `@types/bcrypt`, `@types/jsonwebtoken`:**
   - **কেন ব্যবহার করা হয়েছে:** এক্সপ্রেস, নোড ও অন্যান্য লাইব্রেরির জন্য TypeScript টাইপ ডেফিনিশন ও অটো-কমপ্লিশন প্রদান করে।

### ডেভেলপমেন্ট ডিপেন্ডেন্সিস (Dev Dependencies):

1. **`tsx` (`^4.23.13`):**
   - **কেন ব্যবহার করা হয়েছে:** ডেভেলপমেন্ট চলাকালীন কোনো আলাদা ম্যানুয়াল কম্পাইলেশন ছাড়াই সরাসরি TypeScript ফাইল রান এবং ওয়াচ করতে ব্যবহৃত হয় (`tsx watch src/server.ts`)।
2. **`nodemon` (`^3.1.14`):**
   - **কেন ব্যবহার করা হয়েছে:** ফাইল সেভ করার সাথে সাথে ডেভেলপমেন্ট সার্ভার অটোমেটিক রিস্টার্ট করার জন্য।

---

## ৭. সিকিউরিটি ও ভ্যালিডেশন মেকানিজম

1. **Password Security (Bcrypt):**
   - পাসওয়ার্ড ডেটাবেসে কখনো ওপেন টেক্সট হিসেবে থাকে না। রেজিস্ট্রেশনের সময় ১০ রাউন্ড সল্টিং দিয়ে হ্যাশ করা হয়।
2. **Token Security (JWT):**
   - টোকেনের মেয়াদ ৭ দিন (`JWT_EXPIRES_IN=7d`)। প্রতিবার রিকোয়েস্ট আসার পর সিক্রেট কি দিয়ে সিগনেচার যাচাই করা হয়।
3. **Smart CORS Management:**
   - `src/app.ts`-এ CORS এমনভাবে সাজানো হয়েছে যাতে Vercel-এর যেকোনো ডোমেন (`*.vercel.app`), লোকালহোস্ট বা নির্দিষ্ট অনুমোদিত ডোমেন ছাড়া অন্য কোনো বহিরাগত সাইট রিকোয়েস্ট পাঠাতে না পারে।
4. **Safe Query Sanitization:**
   - সার্চ কুয়েরির ক্ষেত্রে Mongoose-এর বিল্ট-ইন স্যানিটাইজেশন ব্যবহার করায় NoSQL ইনজেকশন থেকে সুরক্ষিত।

---

## ৮. এনভায়রনমেন্ট ভেরিয়েবলস নির্দেশিকা (Environment Variables Guide)

আপনার Render ড্যাশবোর্ডে নিচের ভেরিয়েবলগুলো সক্রিয় রয়েছে:

```env
# সার্ভার কনফিগারেশন
PORT=10000
NODE_ENV=production

# ডেটাবেস সংযোগ (MongoDB Atlas)
MONGODB_URI=mongodb+srv://my-keeps:Tuqlj3SWa7nonsxC@asmual.4icepzp.mongodb.net/my_keeps?appName=Asmual

# ফ্রন্টএন্ড সংযোগ (CORS)
CLIENT_URL=https://my-keeps-pink.vercel.app,http://localhost:3000

# অথেন্টিকেশন সিক্রেট
JWT_SECRET=supersecret_jwt_key_mykeeps_2026
JWT_EXPIRES_IN=7d
```

---

## ৯. রেন্ডার ডেপ্লয়মেন্ট কনফিগারেশন (Render Deployment Workflow)

Render ক্লাউডে অ্যাপ্লিকেশনটি যেন কোনো ম্যানুয়াল এরর ছাড়া বিল্ড ও রান হতে পারে, তার জন্য নিচের সিস্টেম কার্যকর রয়েছে:

1. **`render.yaml`:**
   - সার্ভারের জন্য Blueprint তৈরি করা আছে, যা সরাসরি Render বুঝতে পারে।
2. **অটোমেটিক বিল্ড লাইফসাইকেল (`package.json`):**
   - `"postinstall": "npm run build"` -> Render যখনই প্যাকেজ ইনস্টল করে, ইনস্টলেশনের সাথে সাথে `tsc` রান হয়ে `dist/` ফোল্ডারে জাভাস্ক্রিপ্ট কোড কম্পাইল হয়ে যায়।
   - `"prestart": "npm run build"` -> সার্ভার স্টার্টের আগেও কোড রেডি কিনা চেক করে।
   - `"start": "node dist/server.js"` -> কম্পাইল করা কোড নির্বিঘ্নে চালু করে।
3. **`dist/` কমিট ট্র্যাকিং:**
   - `.gitignore` থেকে `dist/` বাদ দিয়ে প্রি-কম্পাইল্ড কোড গিটহাবে রাখা হয়েছে, যার ফলে Render-এ কখনো `Cannot find module dist/server.js` এরর আসবে না।

---

**সংকলক:** Antigravity AI  
**সর্বশেষ আপডেট:** সেপ্টেম্বর ২০২৬
